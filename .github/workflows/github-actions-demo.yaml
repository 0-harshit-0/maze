name: Maze Generation API
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  repository_dispatch:
    types: [generate]
jobs:
  run:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - env:
          MESSAGE: ${{ github.event.client_payload.msg }}
        run: return $MESSAGE